{
  "name": "coffeenow",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test": "meteor test --driver-package practicalmeteor:mocha --port 4000"
  },
  "dependencies": {
    "meteor-node-stubs": "~0.2.0"
  }
}
